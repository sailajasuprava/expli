import React from "react";
import ReactDOM from "react-dom/client";
import Viewer from "./Viewer";
import "../index.css"; // or viewer.css for now

ReactDOM.createRoot(document.getElementById("root")).render(<Viewer />);
